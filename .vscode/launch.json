{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/testchain/target/debug/node-template",
            "args": [
                "--dev"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}